/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
        '1000logos.net',
        'links.papareact.com',
        'fakestoreapi.com'
    ],
  }
}

module.exports = nextConfig
